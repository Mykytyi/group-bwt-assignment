const jsonFile = /^\w+.json$/;

const NATURAL = 'natural';

const JURIDICAL = 'juridical';

const CASH_IN = 'cash_in';

const CASH_OUT = 'cash_out';

module.exports = {
	jsonFile,
	NATURAL,
	JURIDICAL,
	CASH_IN,
	CASH_OUT
};
